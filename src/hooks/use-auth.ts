
// Este archivo funciona como alias para mantener compatibilidad con importaciones existentes
import { useAuth } from '@/context/AuthContext';

// Re-exportamos el hook desde su ubicación original
export { useAuth };
